# 性能与多模型载入内存优化精简计划书

> 项目：`D:\OYSM\openysm-26.1.2`  
> 范围：模型加载峰值、纹理内存、GPU/direct buffer、渲染热路径  
> 结论：先做 P0，再扩展到 Lazy/LRU，不建议一开始重写渲染器。

## 一、目标

降低大量模型同时载入时的 Java Heap、Direct Memory、GPU 资源峰值，减少长期常驻内存和渲染热路径开销，同时保留 GPU Renderer、Native Renderer、Java fallback。

## 二、现状依据

当前项目已有：

```text
ClientModelManager 单线程模型解析池
OuterFileTexture 持有纹理 byte[]
GpuRenderPath / GpuMesh GPU 渲染路径
GpuMesh.dispose() 可释放 GL buffer、VAO、native pointer、perFrameBoneBuffer
ModelUploadSession / ServerModelManager 导入链路
```

主要风险：

```text
Files.readAllBytes + YsmCrypt.read + parse 可能同时持有多份大 byte[]
OuterFileTexture.data 是 final byte[]，上传 GPU 后仍长期占用 heap
GpuMesh 每模型持有 perFrameBoneBuffer direct buffer
modelAssemblyMap 长期持有完整 ModelAssembly
GPU 状态绑定每次渲染重复执行
```

## 三、实施阶段

### P0：稳定性和可观测

1. 新增 `ModelMemoryProfiler`，默认关闭。
2. 日志覆盖读取、解密/解压、解析、纹理解码、纹理上传、GPU Mesh 构建、加入缓存、卸载。
3. 缩短 `fileBytes`、`decompressed`、导入 `data` 的生命周期。
4. 避免 lambda / future 长时间捕获大 byte[]。
5. `OuterFileTexture` 上传成功后，在配置开启时释放 Java byte[]。
6. 检查模型移除、退出世界、资源重载时是否释放纹理、GpuMesh、direct buffer。

### P1：减少渲染和加载浪费

1. 对资源站下载、模型导入、解析、纹理上传做明确限流。
2. GPU 渲染路径增加帧级状态缓存，减少重复 `glUseProgram`、`bindTexture`、`bindSampler`、`bindSSBO`。
3. 纹理加载时生成 `AlphaProfile`，纯不透明模型跳过透明 pass。
4. 检查 Java fallback / Native fallback 热路径，复用 Matrix、Vector、数组等临时对象。

### P2：模型生命周期管理

1. Lazy Loading：进服只建立模型索引，附近玩家、当前玩家、GUI 预览再完整加载。
2. `ModelAssembly` / `Texture` / `GpuMesh` 加 LRU 卸载。
3. 远离玩家、长期不用、超过模型数量或内存预算时释放完整模型。
4. 本地玩家当前模型、正在渲染模型、GUI 正在预览模型不可淘汰。

### P3：高风险优化

1. Shared `BoneUploadScratch` 或 `BoneRingBuffer`。
2. GPU Mesh 构建成功后，在低内存模式下释放 CPU baked mesh。
3. GeoModel compact 化。
4. Compute Skinning。

## 四、验收标准

```text
单模型加载、切换、资源重载正常
20/50/100 个模型加载时 heap 峰值下降
Direct Memory 不持续增长
退出世界后 GPU/direct 资源可释放
GPU Renderer / Native Renderer / Java fallback 都可用
Iris / Shaderpack 不崩溃
```

## 五、明确不做

```text
不使用 System.gc() 作为核心方案
不删除 fallback
不在后台线程调用 OpenGL
不一次性重写模型格式
不先做多线程渲染
```

## 六、2026-05-31 执行记录

### 已完成 P0

1. 新增 `ModelMemoryProfiler`，默认关闭。
2. 日志覆盖模型缓存读取、解密、二进制解析、客户端映射、Assembly 构建、纹理解码、纹理上传、GPU Mesh 构建、GPU Mesh 释放、GPU/native cache LRU 释放。
3. 缩短 `fileBytes`、`decompressed`、导入 `data` 的引用生命周期。
4. `OuterFileTexture` 上传成功后支持按配置释放 Java 侧图片 `byte[]`。
5. 模型替换、同步移除、LRU cache trim 路径统一释放纹理、native cache、GPU mesh/direct buffer。

### 已完成 P1

1. GPU 渲染路径增加轻量 GL 状态缓存，减少重复 program、texture、sampler、SSBO 绑定。
2. 复用已有半透明扫描结果，纯不透明纹理在 GPU renderer 下跳过透明 pass。
3. Java fallback 路径复用矩阵、向量、骨骼数组 scratch，减少热路径临时对象分配。

### 已完成 P2（保守实现）

1. 新增默认关闭的 GPU/native cache LRU。
2. LRU 只释放未使用模型的 GPU/native render cache，不移除 `ModelAssembly` 本体，避免当前同步协议下按需重拉模型缺口。
3. GUI 打开时不淘汰；当前世界玩家使用中的模型和 `default` 模型不淘汰。

### 配置与界面

新增配置分组 `ExperimentalTesting`，配置界面新增大项“实验性测试”：

```text
模型内存分析日志
纹理上传后释放字节缓存
GPU 缓存模型上限
未使用模型缓存保留时间
```

`GPU 渲染器` 选择保持在原“性能”分组。

### P3 评估

```text
优先级 1：Shared BoneUploadScratch / BoneRingBuffer
    值得做。当前每个 GpuMesh 常驻 perFrameBoneBuffer，模型数量多时 direct memory 常驻明显。

优先级 2：GeoModel compact 化
    先设计和基准测试，不直接大改。当前 BakedBone/BakedCube/BakedQuad 对象图会带来内存和 GC 压力，但改动面覆盖构建、fallback、native、GPU。

暂不推荐：GPU Mesh 构建成功后释放完整 CPU baked mesh
    风险高。Java fallback、native fallback、GPU/native cache 重建都依赖 bakedBones，直接释放会破坏 fallback。

暂不推荐：Compute Skinning
    风险最高。当前 GPU renderer 已有 vertex shader skinning 路径，compute skinning 需要重新处理 xform buffer、barrier、shader pack/Iris 兼容。
```

### 验证

```text
:fabric:compileJava 成功
:fabric:remapJar 成功
导出产物：fabric/build/libs/fox-model-loader-fabric-1.2.0.jar
```
